# Replit Deployment
Import this ZIP or GitHub repo into Replit. Backend run command: `cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000`.
For frontend, open shell: `cd frontend && npm install && npm run dev`.
