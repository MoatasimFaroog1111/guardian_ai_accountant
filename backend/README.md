# Backend
FastAPI backend for Guardian AI Accountant Phase 1.

Run:
```bash
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000
```
No real Odoo posting is implemented in Phase 1.
